# AGENTS.md

本文件为编码智能体提供项目特定的操作规则。保持简洁，仅在持久性约定发生变化时更新。

## 项目概览

- 产品：托管销售与运营系统，包含公开网站、客户控制台、管理控制台、API、MySQL、Prisma 和工作任务。
- 前端：`frontend/`，React + Vite，单一入口 `frontend/src/main.jsx`，样式文件 `frontend/src/styles.css`。
- 后端：`backend/`，Express API 入口 `backend/src/app.js`，Prisma 模型文件 `backend/prisma/schema.prisma`。
- 部署/开发栈：Docker Compose 项目名称需显式指定：`docker compose -p jiyun up -d --build`。
- 验收测试：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts/acceptance.ps1`。

## 产品质量标准

- 将本项目视为可投入运营的产品，而非静态演示。禁止交付硬编码的仪表盘数据、虚假列表状态或无效按钮。
- 管理和客户工作流必须闭环：订单、服务器、工单、钱包、通知和操作日志应保持一致。
- 如果用户操作创建或修改了后端状态，对应的前端列表/详情视图必须清晰地展现该变化。
- 避免在公开前端暴露仅限管理员查看的说明或种子数据/内部实现文本。

## 关键工作流规则

- 余额不足时必须阻止订单创建；余额不足时不得创建未支付的占位订单。
- 已支付且待开通的 `new_server` 订单必须可从管理端的订单/服务器流程中操作。
- 管理端订单消息是通过 `relatedOrderId` 关联的 `Notification` 记录；客户必须在订单详情/列表和通知中能看到这些消息。
- 客户通知必须支持标记/已读。进入通知视图不应让所有消息停留在未读状态。
- 已开通的订单必须链接到客户的服务器视图。客户服务器卡片应显示产品、IP、操作系统、登录信息、到期时间、状态和相关订单。
- 工单需要使用中文状态标签，管理端回复应生成客户通知。
- 管理端用户列表应展示运营相关上下文，而非仅显示原始账户字段：余额、状态和数量统计很重要。

## UI 约定

- 公开页面的登录状态必须明确：分别显示已登录用户、客户中心、退出登录。
- 状态和类型使用中文业务标签，而非原始值如 `new_server`、`unpaid`、`pending`、`none`。
- 数据密集的管理页面可以紧凑，但必须保持可快速浏览：身份单元格、状态标签、紧凑的统计数据和清晰的操作按钮。
- 客户页面应包含从订单到服务器、从通知/订单消息到相关业务对象的直接导航（在可行的情况下）。
- 前端修改后，请通过浏览器验证，而非仅检查构建输出。

## 交付前验证

- 前端编辑后，在 `frontend/` 中运行 `npm run build`。
- Prisma 模型变更后，运行 `npm run prisma:generate` 或重新构建后端。
- 后端/模型/工作流变更时，运行 Docker 和验收脚本。
- 重要流程需进行浏览器验证：登录/注册、购买、订单支付/开通、管理端订单消息、客户订单详情、服务器视图、工单回复、通知已读状态。
- 仅在相关构建、验收和浏览器检查通过后再提交和推送。

## Git 与数据规范

- 不要重置或回退用户的更改。
- 除非明确要求，否则不要提交截图、`test-results/`、`dist/` 或本地环境文件。
- 验收测试会创建测试用户/产品/订单；脚本会处理产品清理。不要手动删除无关的用户数据。
- 保持后续编辑聚焦；除非任务需要，避免对单文件前端进行大范围重写。
